import React from "react";
const StatusChange = (Layout, Button) => ({ selected: selectedIn, onChange, status }) => {
    const [selected, setSelected] = React.useState(selectedIn);
    const [loading, setLoading] = React.useState(false);
    const handleChange = async (id) => {
        setLoading(true);
        await onChange(id);
        setSelected(id);
        setLoading(false);
    };
    return (React.createElement(Layout, null, status.map(({ id, label }, i) => (React.createElement(Button, { disabled: loading, key: i, handleChange: handleChange, selected: selected, id: id, label: label })))));
};
export default StatusChange;
