/// <reference types="react" />
export interface LayoutProps {
    children: JSX.Element[];
}
export interface ButtonProps {
    disabled?: boolean;
    selected?: number;
    id: number;
    label: string;
    handleChange: (s: number) => Promise<void>;
}
export interface StatusChangeProps {
    selected?: number;
    onChange: (s: number) => Promise<void>;
    status: {
        id: number;
        label: string;
    }[];
}
declare const StatusChange: (Layout: (l: LayoutProps) => JSX.Element, Button: (l: ButtonProps) => JSX.Element) => ({ selected: selectedIn, onChange, status }: StatusChangeProps) => JSX.Element;
export default StatusChange;
