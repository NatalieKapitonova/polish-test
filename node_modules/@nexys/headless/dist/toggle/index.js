import React from 'react';
const ToggleHeadless = (structure, Form, View, LayoutView, LayoutForm) => ({ data: dataIn }) => {
    const [data, setData] = React.useState(dataIn);
    const [isForm, setIsForm] = React.useState(false);
    if (isForm) {
        return (React.createElement(LayoutForm, { setIsForm: setIsForm },
            React.createElement(Form, { data: { dataIn: data }, onSuccess: d => {
                    setData(d);
                    setIsForm(false);
                } })));
    }
    return (React.createElement(LayoutView, { setIsForm: setIsForm },
        React.createElement(View, { data: data, structure: structure })));
};
export default ToggleHeadless;
