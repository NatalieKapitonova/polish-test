import React from "react";
const BadgeActionHeadless = (Badge, Loader, CopyIcon) => (label, getActionResult) => ({ id }) => {
    const [isLoading, setIsLoading] = React.useState(false);
    const handleAction = async (id) => {
        setIsLoading(true);
        const actionResult = await getActionResult(id);
        await navigator.clipboard.writeText(actionResult);
        alert("copied to clipboard");
        setIsLoading(false);
    };
    if (isLoading) {
        return React.createElement(Loader, null);
    }
    return (React.createElement("button", { onClick: () => handleAction(id) },
        React.createElement(Badge, null,
            React.createElement(React.Fragment, null,
                label,
                " ",
                React.createElement(CopyIcon, null)))));
};
export default BadgeActionHeadless;
