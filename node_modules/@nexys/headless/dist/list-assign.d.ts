/// <reference types="react" />
import { ListWrapperProps } from './table/ui-type';
export interface UnitUIProps {
    isChecked: boolean;
    label: string;
    isLoading: boolean;
    onChange: () => void;
}
export interface Item<SId> {
    id: SId;
    assigned: boolean;
    name: string;
}
export declare type UpdatePromise<StatusId> = (id: StatusId, assigned: boolean) => Promise<boolean | void>;
declare const ListAssign: (ListWrapperUI: (p: ListWrapperProps) => JSX.Element, UnitUI: (p: UnitUIProps) => JSX.Element) => <StatusId>({ data, updatePromise }: {
    data: Item<StatusId>[];
    updatePromise: UpdatePromise<StatusId>;
}) => JSX.Element;
export default ListAssign;
