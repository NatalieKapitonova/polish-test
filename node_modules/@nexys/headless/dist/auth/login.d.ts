/// <reference types="react" />
interface OnSuccessProps<Profile> {
    profile: Profile;
    permissions: string[];
    locale: string;
    redirectUrl?: string;
}
/**
 * after call to backend, login user on the client side
 */
export declare const RedirectSuccess: <Profile>({ profile, permissions, locale, redirectUrl, }: OnSuccessProps<Profile>) => JSX.Element;
export declare const Login: (Spinner: () => JSX.Element) => <Profile, LoginPayload = any>({ loginPayload, apiCall, }: {
    loginPayload: LoginPayload;
    apiCall: (payload: LoginPayload) => Promise<OnSuccessProps<Profile>>;
}) => JSX.Element;
export {};
