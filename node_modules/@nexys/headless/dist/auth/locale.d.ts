export declare type Locale = string;
export declare const localeDefault: Locale;
export declare const langToLocale: (lang: string, country?: string) => Locale;
/**
 * turn locale into lang and country
 * @param locale : typically "fr_CH", lang only is also accepted, e.g. "en"
 * @returns {lang, country}
 */
export declare const localeToLangAndCountry: (locale: string) => {
    lang: string;
    country: string;
};
