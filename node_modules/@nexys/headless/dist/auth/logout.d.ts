/**
 * backend logout
 * @returns
 */
export declare const backend: (url?: string) => Promise<number>;
/**
 * logs user out
 * client (local storage) + server (clean tokens)
 */
export declare const user: (backendUrl?: string) => void;
