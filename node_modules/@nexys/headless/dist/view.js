import React from 'react';
const LayoutMinimal = (p) => React.createElement(React.Fragment, null, p.children);
const View = (Row, Layout = LayoutMinimal) => ({ data, structure, title, description }) => (React.createElement(Layout, { title: title, description: description }, structure.map((s, i) => {
    const value = typeof s.value === 'function'
        ? s.value(data)
        : String(data[s.value]);
    return React.createElement(Row, { key: i, label: s.label, value: value });
})));
export default View;
