export const isDateRangeDefined = (d, errors) => Object.keys(errors).length === 0;
export const isETHAddress = (s) => {
    const r = s.match(/^0x[^ ]+$/);
    if (!r) {
        return false;
    }
    return r.length > 0;
};
