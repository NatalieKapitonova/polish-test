import { DateRange, ErrorsDateRange } from "./type";
export declare const isDateRangeDefined: (d: Partial<DateRange>, errors: ErrorsDateRange) => d is DateRange;
export declare const isETHAddress: (s: string) => boolean;
