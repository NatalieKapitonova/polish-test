import React from "react";
const FormUIGenerator = ({ InputGeneric, InputWrapper, Button }) => (def) => ({ form, setForm, loading, errors }) => {
    return (React.createElement(React.Fragment, null,
        def.map((item, i) => {
            const Input = InputGeneric(item.uiType);
            return (React.createElement(InputWrapper, { errors: errors[item.name], key: i },
                React.createElement(Input, { value: form[item.name], onChange: (val) => setForm({ ...form, [item.name]: val }), disabled: loading, placeholder: item.label, errors: errors[item.name] })));
        }),
        React.createElement(Button, null)));
};
export default FormUIGenerator;
