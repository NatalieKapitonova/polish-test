import { FormDef } from './type';
import * as Validation from '@nexys/validation';
export declare const enumToOptions: <A>(keys: {
    [s: number]: string;
}) => {
    id: A;
    name: string;
}[];
export declare const isA: <A>(a: Partial<A>, formErrors: { [k in keyof A]?: string[] | undefined; }) => a is A;
export declare const defToShape: <A>(def: FormDef<A>[]) => Validation.Type.Shape;
