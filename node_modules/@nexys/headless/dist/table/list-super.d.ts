import React from "react";
import { Config, Definition, AsyncDataConfig, AsyncDataReturn } from "../types";
import { OuterProps } from "./ui-type";
export interface InnerProps<A> {
    def: Definition<A>;
    data?: A[];
    nPerPage?: number;
    config?: Config<A>;
    asyncData?: (config: AsyncDataConfig<A>) => Promise<AsyncDataReturn<A>>;
    CustomListContainer?: (children: React.ReactNode) => JSX.Element;
    CustomListItem?: (rowData: A) => JSX.Element;
}
declare const ListSuper: <A>({ HeaderUnit, FilterUnit, OrderController, ColCell, GlobalSearch, NoRow, Row, ListWrapper, ListContainer, ListHeader, ListBody, RecordInfo, Pagination, Loader, }: OuterProps<A>) => (props: InnerProps<A>) => JSX.Element;
export default ListSuper;
