import { FiltersType } from '../../types';
import { State } from './type';
declare const getInitialState: <A>(data?: A[], sortAttribute?: keyof A | undefined, sortDescAsc?: boolean, filters?: FiltersType<A>) => State<A>;
export default getInitialState;
