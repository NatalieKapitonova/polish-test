import React from 'react';
import { Navigate } from 'react-router-dom';
import BodyHeadless from './body';
const Layout = ({ Card, Tabs, Navs, Header, Col, Row, BackBtn = () => React.createElement(React.Fragment, null) }) => {
    return ({ cards, title, description, backRedirect, pathPrefix = '' }) => ({ data: dataIn }) => {
        // picks either nav or tabs. At least one of the two must be chosen
        const Body = BodyHeadless({ Card, Col, Row, Tabs, Navs });
        const [redirect, setRedirect] = React.useState();
        const [data, setData] = React.useState(dataIn);
        if (redirect) {
            return React.createElement(Navigate, { to: redirect });
        }
        return (React.createElement(React.Fragment, null,
            React.createElement(Header, { title: title, description: description }),
            React.createElement(Body, { pathPrefix: pathPrefix, cards: cards, data: data, setData: setData }),
            backRedirect && (React.createElement(BackBtn, { onClick: () => setRedirect(backRedirect) }))));
    };
};
export default Layout;
