/// <reference types="react" />
import * as T from './type';
declare const Layout: ({ Card, Tabs, Navs, Header, Col, Row, BackBtn }: T.LayoutOuterProps) => <A>({ cards, title, description, backRedirect, pathPrefix }: T.Layout<A>) => ({ data: dataIn }: {
    data: A;
}) => JSX.Element;
export default Layout;
